# Uploads

Plaats hier foto's of andere bestanden die je op de site wilt gebruiken (bijvoorbeeld voor de galerij-pagina). Verwijs er in de HTML naar met een relatief pad, bijvoorbeeld:

```html
<img src="../uploads/foto.jpg" alt="...">
```

(vanuit een pagina in `pages/`) of `uploads/foto.jpg` vanuit `index.html`.
